import { InputFormattingRefDirective } from './input-formatting-ref.directive';

describe('InputFormattingRefDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFormattingRefDirective();
    expect(directive).toBeTruthy();
  });
});
